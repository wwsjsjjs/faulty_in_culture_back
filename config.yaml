app:
  environment: development  # 环境：development/production
  port: "8080"              # 服务端口
  log_mode: dev             # 日志模式：dev/prod
  gin_mode: debug           # Gin模式：debug/release

database:
  host: 127.0.0.1
  port: 3306
  user: root
  password: 1234
  name: culture
  charset: utf8mb4
  auto_create: true         # 自动创建数据库（如果不存在）

redis:
  host: 127.0.0.1
  port: 6379
  password: ""
  db: 0

ai:
  api_key: ""               # 腾讯混元AI API密钥（也可使用环境变量HUNYUAN_API_KEY）
  base_url: "https://api.hunyuan.cloud.tencent.com/v1"
  model: "hunyuan-lite"     # 使用的模型

jwt:
  secret: "faulty_in_culture_secret_key_2026"  # JWT 签名密钥（生产环境请使用环境变量）
  expire_hours: 168          # Token 过期时间（小时，168小时=7天）
